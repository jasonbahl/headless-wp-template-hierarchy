# WPGraphQL Template Hierarchy Debugger

This is a project to demonstrate how to re-create the WordPress template hierarchy with Headless WordPress using WPGraphQL.

## Examples

- Express.js (see the `/express` directory)
- Gatsby.js (future)
- NextJS (future)
- Remix Run (future)
